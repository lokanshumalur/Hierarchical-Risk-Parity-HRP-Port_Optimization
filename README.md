# Hierarchical-Risk-Parity-HRP-Port_Optimization
Implements the Hierarchical Risk Parity (HRP) algorithm, addressing the "Mean-Variance Curse" by using machine learning clustering (unsupervised learning) to build a robust portfolio that does not require the inversion of a covariance matrix.
